# データベースへの接続設定
spring.datasource.sql-script-encoding   = UTF-8
spring.datasource.driver-class-name     = com.mysql.cj.jdbc.Driver
spring.datasource.url                   = jdbc:mysql://rds-mysql-server-headbanding.cmta6klhdst4.ap-northeast-1.rds.amazonaws.com/Javatter
spring.datasource.username              = javatter
spring.datasource.password              = javatter20200609

# src/main/resources/data.sql をサーバー起動時に毎回実行する設定
spring.datasource.initialization-mode   = always

# spring securityの認証設定
spring.security.user.name               = admin
spring.security.user.password           = javatter

# spring Boot 2.2.0に更新したときにput/deleteメソッドが動かない問題を対処
spring.mvc.hiddenmethod.filter.enabled  = true

# アプリケーション起動時にデータベースを初期化しないようにする
spring.jpa.hibernate.ddl-auto           = update
